# README

There are some benchmarks that require individual environments.

- `HumanEvalFix`: download testsuites and install the environment `humanevalfix` following the [official repository](https://github.com/bigcode-project/bigcode-evaluation-harness).
- `CanItEdit`: please install the environment `canitedit` following `CanItEdit/README.md`.
